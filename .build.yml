image: alpine/edge
packages:
  - nodejs
  - yarn
artifacts:
  - obsidian-bibeltext-plugin.tar.gz
tasks:
- setup: |
    yarn install
- build: |
    npm build
- package: |
    tar -c manifest.json main.js styles.css | gzip > obsidian-bibeltext-plugin.tar.gz

